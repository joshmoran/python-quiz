# def check_correct( total_quizzes, user_input ):
#   try:
#       selected_quiz = int(input('Please select a number to start a quiz:         '))
#       available_quizzes = range(1, len(available_quizzes))
      
#       while selected_quiz not in available_quizzes:
#           selected_quiz = int(input('Please select a number to start a quiz:         '))
#       #     raise ValueError
#       # else:
#       #     quiz_file = files[selected_quiz - 1]
#       #     print(f'Starting quiz: {quiz_file}')
#   except ValueError:
#       print('Invalid input. Please enter a number between 1 and', len(files))
#       load()



# Create Interface and execute functions 
# load()

# quiz_select = int( input( 'Please select a quiz ') )

# print(f"Number selected is {quiz_select} ")
# while quiz_select < 1 and quiz_select > len( available_quizzes) and type(quiz_select) != 'int':
#   quiz_select = int( input( 'Please select a quiz ') )

# input('Please select a number to start a quiz:         ')

# print( files )